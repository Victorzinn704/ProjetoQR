{
  "name": "projeto-qr",
  "version": "1.0.0",
  "private": true,
  "description": "Cartão digital profissional publicado no GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-source.mjs",
    "generate:qr": "node scripts/generate-qr.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "node scripts/visual-check.mjs",
    "preview": "npx --yes serve dist"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "qrcode": "^1.5.4"
  }
}
